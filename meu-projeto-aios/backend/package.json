{
  "name": "control-tower-backend",
  "version": "1.0.0",
  "description": "Control Tower Executivo - Backend API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "echo \"Backend is Node.js (no build needed)\" && exit 0",
    "lint": "eslint . --ext .js --fix 2>/dev/null || true",
    "test": "node --test 'tests/**/*.test.js' 2>/dev/null || echo 'Tests not configured'",
    "test:watch": "node --watch --test 'tests/**/*.test.js' 2>/dev/null || true"
  },
  "keywords": ["control-tower", "dashboard", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "@supabase/supabase-js": "^2.95.3",
    "axios": "^1.13.5",
    "dotenv": "^17.3.1",
    "fastify": "^5.7.4",
    "fastify-rate-limit": "^1.1.1",
    "node-cron": "^4.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
