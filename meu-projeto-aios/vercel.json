{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && cp -r frontend/dist public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/:path((?!.*\\.).*)*", "destination": "/index.html" }
  ]
}
