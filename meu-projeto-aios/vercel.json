{
  "buildCommand": "npm run build && mkdir -p public && cp -r frontend/dist/* public/",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
