# Story 2.2: Catálogo parceiros → destinos → programas

## Status
Done — seed 016 aplicado (12 parceiros/6 destinos/5 programas reais, sem fallbacks), UI do catálogo no deploy, validação contra a BD

## Story
**Como** Ana,
**quero** gerir parceiros, destinos e programas com custos e comissões num catálogo navegável,
**para** que o agente e a equipa citem sempre informação correcta (faixas, moedas, % de comissão, brochuras).

## Acceptance Criteria
1. CRUD completo dos 3 níveis (`parceiros` → `destinos` → `programas`) na rota `app/(app)/catalogo/`, com faixas de custo (`custo_min`/`custo_max`), moeda ISO 4217 (`CHAR(3)` validada por regex — o formulário só aceita 3 letras maiúsculas), `% comissão` (base no parceiro + override opcional no programa, `comissao_percent NUMERIC(5,2)`) e links de brochuras (`brochura_url`, `link`, `website`).
2. Catálogo semeado via migração **016 `016_seed_catalogo.sql`** com os parceiros já enviados pelo cliente: Kaplan, INTO, MPW, Inspired, IH Cape Town, SAMIAD, U. Europeia/IADE/IPAM, GEA, Xior, EDU4WORD + destinos top-6 (rollback `DELETE`).
3. Pesquisa e filtro por destino (país) e por tipo de programa (`programa_tipo`: summer_camp, linguas, foundation, licenciatura, mestrado, voluntariado, outro), servidos pelos índices `idx_destinos_pais` e `idx_programas_tipo`.
4. Desactivação suave em vez de apagamento cego: `is_active` togglável em parceiros e programas; registos inactivos escondidos das listas por defeito mas preservados (FKs de candidaturas/financeiro não quebram).
5. Escrita no catálogo respeita a RLS por papel (§8.1): leitura `authenticated`, `DELETE` só admin.

## Tasks / Subtasks
- [ ] Escrever e aplicar migração `016_seed_catalogo.sql` com os 10 parceiros + destinos top-6 (AC: 2)
  - [ ] Preencher `tipo`, `comissao_percent`, `website`/`brochura_url` com os dados reais enviados pelo cliente; campos sem informação ficam NULL (sem valores inventados)
  - [ ] Rollback `DELETE` idempotente
- [ ] Construir páginas do catálogo em `app/(app)/catalogo/` (AC: 1, 4)
  - [ ] Lista de parceiros → drill-down destinos → drill-down programas (hierarquia visível)
  - [ ] Formulários criar/editar com validação de moeda (3 letras maiúsculas) e faixas min≤max
  - [ ] Toggle `is_active` com confirmação
- [ ] Pesquisa e filtros (AC: 3)
  - [ ] Filtro por país (destinos) e por `tipo` (programas); pesquisa por nome
- [ ] Verificar RLS nas operações da UI (delete falha para papel operação) (AC: 5)

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §2.5](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) (colunas exactas das 3 tabelas), §3 (migração 016), §8.1 (RLS). PRD §3 — ecrã nuclear nº5 (Catálogo).
- **Dependência:** story 2.1 (migração 008 cria as tabelas; esta story NÃO cria schema — só semeia e constrói UI).
- **Seed 016 pertence a esta story; o seed 015 (agente) pertence ao Épico 3** — apesar de §10.1 listar 015–016 juntas no E2, o agente só faz sentido com o schema da migração 004 + prompt do E3.
- **O catálogo alimenta o agente (E3):** as tabelas de universidades/faixas de investimento das 12 FAQ (FR2) e a regra de faixas (FR3) leem daqui — faixas, nunca valores exactos de cotação.
- **Consumidores a jusante:** `candidaturas.programa_id/parceiro_id` (2.3), `fichas_estudante.programa_pretendido_id` (2.4), `financeiro.parceiro_id` + `percentagem` (2.6), `v_destinos_receita` (2.7).

### Testing
- SQL pós-seed: `SELECT count(*) FROM parceiros` = 10; `SELECT count(*) FROM destinos` ≥ 6; nomes conferidos um a um contra a lista do cliente.
- `INSERT INTO destinos (..., custo_vida_currency) VALUES (..., 'eur')` → falha (regex maiúsculas).
- node:test dos helpers de validação do formulário (moeda, min≤max) se extraídos para `lib/`.
- Smoke UI: criar parceiro → destino → programa → aparece no filtro por tipo; desactivar → some da lista, registo mantém-se na BD.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E2 + Arquitectura v1.0 | River (@sm) |
| 13/07/2026 | 1.1 | Seed 016 + UI catálogo (@dev). Aplicado ao Supabase real + validado + deploy pela coordenação. Story Done. | Dex (@dev) |

## Dev Agent Record
### Agent Model Used
@dev (Dex) + coordenação — Claude Opus 4.8

### Completion Notes List
- **Migração `016_seed_catalogo.sql`** escrita e APLICADA (Local=Remote). **12 parceiros** reais do cliente (Kaplan, INTO, MPW, Inspired, IH Cape Town, SAMIAD, U.Europeia, IADE, IPAM, GEA, Xior, EDU4WORD), **6 destinos** (África do Sul, Portugal, RU, EAU, EUA, China), **5 programas** com faixas reais (Summer Camp 1600-2000, Inglês 6m 6000-18000, Foundation 18000-35000, Licenciatura 19000-70000, Mestrado 5700-40000 EUR). **Regra sem fallbacks respeitada**: `comissao_percent`/`website`/`brochura_url` = NULL onde o cliente não deu; validado que comissões estão NULL na BD. Idempotente (NOT EXISTS) + rollback por chave natural.
- **Decisão documentada:** a story previa 10 parceiros tratando U.Europeia/IADE/IPAM como grupo; o @dev semeou 12 distintos (instituições separadas) — dado honesto, verificação ajustada para count=12.
- **UI `src/app/(dashboard)/catalogo/`**: página com hierarquia parceiros→destinos→programas, formulários (parceiro/destino/programa) com validação, modal, toggle is_active, filtros por país/tipo, pesquisa. Link "Catálogo" na sidebar.
- **Helpers `src/lib/catalogo-validation.ts`** (moeda regex, min≤max) + `tests/catalogo-validation.test.mjs` — **10/10 testes passam**.
- **`npm run build` verde**; rota `/catalogo` protegida no deploy (307 sem sessão).

### ACs — estado
- AC1 ✔ CRUD 3 níveis · AC2 ✔ seed aplicado (12/6/5, dados reais) · AC3 ✔ filtros país/tipo (validado foundation via REST) · AC4 ✔ is_active/desactivação suave · AC5 ✔ RLS por papel (DELETE só admin).

### File List
- `supabase/migrations/016_seed_catalogo.sql`
- `src/app/(dashboard)/catalogo/page.tsx`
- `src/components/catalogo/{parceiro-form,destino-form,programa-form,modal}.tsx`
- `src/lib/catalogo-validation.ts` · `tests/catalogo-validation.test.mjs`
- `src/components/layout/sidebar.tsx` (link Catálogo)

## QA Results
Validação directa: seed Local=Remote · 12 parceiros / 6 destinos / 5 programas na BD · comissão NULL (sem fallbacks) · faixa Foundation 18000-35000 EUR correcta · filtro por tipo OK · 10/10 testes de validação · build verde · /catalogo 307 protegida no deploy.
