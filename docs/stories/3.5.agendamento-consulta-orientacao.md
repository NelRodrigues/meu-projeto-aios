# Story 3.5: Agendamento da Consulta de Orientação

## Status
Review

## Story
**Como** lead qualificado,
**quero** marcar a Consulta de Orientação com o Rinaldo directamente na conversa,
**para** avançar sem esperar por trocas de mensagens manuais.

## Acceptance Criteria
1. Tool `check_availability` consulta **FreeBusy do Google Calendar do Rinaldo** dentro de janelas fixas acordadas, com **buffer e fuso explícito**, e devolve 2–3 slots livres ao lead (FR7).
2. ⚠️ **DECISÃO PENDENTE-3 (questão 4 de 14/07, dono: Rinaldo):** janelas/dias/horas/buffer/fuso ainda não fixados — até lá, a tool lê um **placeholder configurável em `ai_sales_agents.settings.consulta_windows`** (editável sem redeploy). A story NÃO bloqueia: implementa-se com o placeholder e actualizam-se os valores após 14/07.
3. Tool `schedule_consultation`: cria o evento no Google Calendar do Rinaldo, move a candidatura para `fase='consulta_agendada'` (com registo em `mudancas_estagio`), **regista a consulta na tabela `consultations` (migração 018, Arquitectura §2.5 — decisão PO C3/D5: `lead_id`, `candidatura_id` opcional, `google_event_id`, `scheduled_at`, `timezone`, `estado='agendada'`)** e confirma na conversa com data/hora + fuso explícito.
4. **Nunca agenda no passado** (NFR10): a camada L0 (data/hora + TZ Africa/Luanda) governa qualquer proposta de slot; slots propostos são sempre futuros e dentro das janelas.
5. Lembrete automático **24h antes** da consulta via cron `lembretes-tick` (mensagem WhatsApp ao lead; controlo por `consultations.lembrete_24h_enviado`); reagendamento pelo mesmo fluxo (`check_availability` → `schedule_consultation`, actualizando a linha de `consultations` e o evento existente via `google_event_id` — nunca duplicando).
6. Falha da API Google (indisponível/token expirado): resposta de continuidade ao lead + escalação para humano com `notificar_humano` — nunca inventa disponibilidade (NFR11).

## Tasks / Subtasks
- [x] Integração Google Calendar (AC: 1, 6) ⚠️ **DEPENDÊNCIA EXTERNA (ponto de ligação):** cliente REST real escrito, mas atrás de `GoogleCredentialsProvider` — a FONTE do token (service account JSON ou access_token em `integration_keys` service='google') liga quando o Rinaldo entregar. Sem credenciais → degrada (continuidade + `notificar_humano`), nunca inventa agenda.
  - [x] Cliente FreeBusy em `_shared/google-calendar.ts` reutilizável pelas duas tools (freeBusy + createEvent + patchEvent)
- [x] Implementar `check_availability` (AC: 1, 2, 4)
  - [x] Interseção FreeBusy × `settings.consulta_windows` (placeholder `[{dia,inicio,fim}]` + `buffer_minutos` + `duracao_minutos` + `timezone` + `horizonte_dias`) → 2–3 slots futuros
  - [x] Fuso explícito na mensagem ao lead ("hora de Luanda" / "Luanda time")
- [x] Aplicar a migração `consultations` (índices + RLS + rollback, DDL na Arquitectura §2.5) (AC: 3, 5) — materializada como **031** (018 nunca existiu como ficheiro; mesmo desvio de numeração da 025, documentado no cabeçalho)
- [x] Implementar `schedule_consultation` (AC: 3, 5)
  - [x] Criar evento no calendar; `candidaturas.fase='consulta_agendada'` + `mudancas_estagio` (via trigger 017); INSERT em `consultations` (`google_event_id`, `scheduled_at`, `timezone`); confirmação na conversa com fuso
  - [x] Reagendamento: localiza a linha viva de `consultations` do lead e faz PATCH do MESMO `google_event_id` + UPDATE da mesma linha (não duplica no calendar nem na BD)
- [x] Lembrete D-1 no cron `lembretes-tick` (AC: 5) — action `lembretes_tick` na edge + migração **032** (cron horário via `cron_config`)
  - [x] Consultas nas próximas 24h com `lembrete_24h_enviado=false` e `estado IN ('agendada','confirmada')` → mensagem WhatsApp + `lembrete_24h_enviado=true` (idempotente; só marca se o envio foi aceite)
- [x] Registar as duas tools em `ai_agent_tools` (seed 029 — story 3.2, já declaradas) e ligar ao loop do `gm-agent` (AC: 1, 3) — executores ligados em `tools-exec.ts`
- [x] Verificação final: lógica §5.3 coberta por node:test (24 casos), incluindo agendar "ontem" → recusada + fora de janela + sem slots → mensagem honesta (AC: 3, 4). Validação ponta-a-ponta com Google real PENDENTE das credenciais do Rinaldo + deploy da edge.

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §5.3](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) — fluxo de agendamento FreeBusy completo + nota PENDENTE-3; §4.2 (cron `lembretes-tick`); §6 (tools e L0); §10.2 (risco "agendar no passado / fuso do Rinaldo em viagem").
- **Dependências:** story **3.4** (o agendamento só se oferece a lead qualificado; tools partilham o loop e `notificar_humano`), 3.1/3.2 (canal + prompt), E2 (tabela `candidaturas`, migração 009). Todas dependem do E1 e da migração 004.
- **Desvio RESOLVIDO (correcção C3/D5 da validação PO):** decisão do @po — **opção (b) fixada: tabela leve `consultations`**, definida na Arquitectura §2.5 e criada pela **migração 018** (com rollback). Fundamento: o dashboard 4.6 ("consultas agendadas") e o lembrete D-1 precisam de fonte consultável distinta da fase; o reagendamento localiza o evento numa linha própria; alinha literalmente com o PRD. A story 4.6 consome esta tabela.
- **PENDENTE-3 não é bloqueio, é configuração:** todo o código lê `settings.consulta_windows`; a validação de 14/07 só muda dados, nunca código. Deixar valores placeholder plausíveis (ex.: ter-qui 14:00–17:00, buffer 15 min, Africa/Luanda) claramente marcados como provisórios.
- **Fuso é risco real (Rinaldo em viagem):** o fuso do calendário pode diferir de Africa/Luanda — normalizar sempre para o TZ definido nos settings e explicitá-lo na confirmação ao lead.

### Testing
- node:test com FreeBusy mockado: janelas × ocupação → slots correctos; sem slots livres → mensagem honesta + oferta de handoff.
- Teste anti-passado: relógio simulado → nenhum slot anterior a `now()` em Africa/Luanda é proposto (regra L0).
- Teste de reagendamento: segunda marcação actualiza o evento e a mesma linha de `consultations` (sem duplicados no calendar nem na BD).
- Teste do lembrete: consulta a <24h → 1 lembrete e apenas 1 (idempotente via `lembrete_24h_enviado`).
- SQL: agendar → `SELECT count(*) FROM consultations WHERE estado='agendada'` = 1 com `google_event_id` e `scheduled_at` preenchidos.
- Teste de falha Google: API devolve erro → resposta de continuidade + `notificar_humano`; nada gravado como agendado.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E3 + Arquitectura v1.0 | River (@sm) |
| 10/07/2026 | 1.1 | Correcção C3 da validação PO (po-validation-backlog-v1) | River (@sm) |
| 13/07/2026 | 1.2 | Implementação completa (AC1–6): cliente Google Calendar atrás de interface de credenciais, tools check_availability/schedule_consultation, migração consultations (031), cron lembretes-tick (032), placeholder consulta_windows (033). 24 testes verdes. Status → Review | Dex (@dev) |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (@dev / Dex)

### Debug Log References
- Testes: `node --test tests/agendamento-consulta.test.mjs` → 24/24 verdes.
- Suíte do agente (agendamento + BANT + escalação + prompt + canal) → 116/116 verdes.
- Falhas em `tests/app/production-smoke.test.mjs` (6/9) são **pré-existentes** (build de produção da app não gerado neste worktree) e independentes da 3.5 — confirmado por `git stash` (falham igual sem as mudanças desta story).
- Sintaxe validada com `node --experimental-strip-types --check` em todos os ficheiros novos/editados (deno não instalado localmente; `deno check` real corre no deploy).

### Completion Notes List
- **Decisão de credenciais (14/07, founder):** "só interface de credenciais agora". O cliente REST do Google (FreeBusy + createEvent + patchEvent) está escrito e definitivo; a FONTE do access token vive atrás de `GoogleCredentialsProvider` (service account JSON → JWT RS256 → token, OU access_token directo, ambos de `integration_keys` service='google'). Enquanto não houver credenciais, o provider devolve `null` e as tools **degradam graciosamente** (continuidade + `notificar_humano`) — **nunca inventam disponibilidade** (AC6/NFR11).
- **Anti-passado (AC4/NFR10):** `generateSlots` exclui qualquer slot ≤ now; `validateChosenSlot` rejeita `slot_iso` no passado (reason=past), fora de janela (out_of_window) ou não-ISO (invalid, com regex ISO-8601 endurecida porque V8 parseia strings livres). Tudo em Africa/Luanda via `tzOffsetMinutes`/`localToUtc` (sem DST, mas geral para o Rinaldo em viagem).
- **Reagendamento (AC5):** localiza a linha viva de `consultations` do lead; se existir `google_event_id`, faz PATCH do mesmo evento e UPDATE da mesma linha (reinicia `lembrete_24h_enviado`) — nunca duplica no calendar nem na BD.
- **Numeração das migrações:** a arquitectura pedia `018_consultations.sql`, mas 018 nunca foi materializada (série real vai até 030) — criada como **031** (mesmo desvio documentado da 025). Cron lembretes = **032**; placeholder consulta_windows = **033**.
- **PENDENTE-3 (janelas do Rinaldo):** placeholder ter–qui 14:00–17:00, buffer 15, dur 45, Africa/Luanda em `settings.consulta_windows` (migração 033, marcado `_provisorio`). Muda-se só o JSON após 14/07, sem redeploy.
- **Lembrete D-1:** cron `lembretes-tick` horário (não diário) para reduzir latência; idempotente pela flag + janela [now, now+24h); só marca a flag se o WhatsApp foi aceite.

### File List
**Novos:**
- `supabase/functions/_shared/google-calendar.ts` — cliente Google Calendar (FreeBusy/eventos) + provider de credenciais (ponto de ligação pendente)
- `supabase/functions/gm-agent/scheduling.ts` — lógica PURA de agendamento (slots, anti-passado, fuso, validação)
- `supabase/functions/gm-agent/reminders.ts` — lógica PURA do lembrete D-1
- `supabase/migrations/031_consultations.sql` — tabela `consultations` + índices + RLS + rollback
- `supabase/migrations/032_cron_lembretes_tick.sql` — cron `lembretes-tick` (action `lembretes_tick`)
- `supabase/migrations/033_consulta_windows_placeholder.sql` — placeholder `settings.consulta_windows` (PENDENTE-3)
- `tests/agendamento-consulta.test.mjs` — 24 testes (slots, anti-passado, reagendamento, fuso, lembrete)

**Editados:**
- `supabase/functions/gm-agent/tools-exec.ts` — executores `check_availability` + `schedule_consultation` + escalação por agenda indisponível
- `supabase/functions/gm-agent/queue-processor.ts` — `handleLembretesTick` (envio D-1)
- `supabase/functions/gm-agent/index.ts` — action `lembretes_tick`
- `supabase/functions/gm-agent/tools.ts` — comentário do placeholder actualizado (só `notificar_humano` resta)

## QA Results
