# Story 2.4: Ficha de estudante 360º

## Status
Done — ficha 360º + checklist + timeline + formulário público validados E2E contra a BD; 2 lacunas resolvidas (enum origem 021, tabela consentimentos 022) + bucket storage 023

## Story
**Como** Ana,
**quero** a ficha completa do estudante num só ecrã (dados, encarregado, documentos, timeline),
**para** tratar documentos e processos sem andar a procurar em conversas de WhatsApp.

## Acceptance Criteria
1. Ficha 360º em `app/(app)/leads/[id]/` sobre a tabela `fichas_estudante` (1:1 com `leads` via `lead_id UNIQUE`): dados pessoais (`nome_completo`, `data_nascimento`, `nacionalidade`), encarregado (`encarregado_nome`, `encarregado_contacto`, `encarregado_relacao`), percurso académico (`percurso_academico`, `nivel_linguistico`), destino/programa pretendido (`destino_pretendido`, `programa_pretendido_id`→catálogo), orçamento (`orcamento_faixa` — faixa declarada, nunca valor exacto).
2. Checklist documental (passaporte, certificados, comprovativos…) sobre `fichas_estudante.documentos JSONB` (`[{tipo, estado, url, updated_at}]`), com estado por documento restrito ao enum `documento_estado`: `em_falta`, `recebido`, `validado`, `rejeitado`.
3. Timeline unificada no mesmo ecrã, ordenada cronologicamente, agregando `interacoes`, `mudancas_estagio` e `mensagens_whatsapp` do lead (emails entram no E5 — deixar o agregador extensível por tipo de evento).
4. Formulário público (link partilhável, rota fora do grupo autenticado) que cria/actualiza lead + ficha directamente, com `lead_origem='formulario_site'`; telefone normalizado com `normalizeAngolaPhone`.
5. Campo `processo_em_curso` visível e editável na ficha, com aviso explícito na UI: ⚠ enquanto `true`, o contacto está **excluído da rotina de retenção de 2 anos** (cron `compliance-2anos`, story 4.4).
6. Secções de financeiro e emails aparecem como painéis do 360º mas alimentam-se das stories 2.6 e 5.x — a ficha não pode quebrar quando essas fontes ainda não existem.

## Tasks / Subtasks
- [x] Página da ficha 360º com secções: dados, encarregado, percurso, destino/programa, orçamento (AC: 1)
  - [x] Selector de programa a partir do catálogo (2.2); campo livre `destino_pretendido` quando ainda sem programa
- [x] Checklist documental sobre o JSONB (AC: 2)
  - [x] Componente de item com transição de estado restrita ao enum `documento_estado` (validação aplicacional em `src/lib/documentos.ts`)
  - [~] Upload opcional do documento para Supabase Storage → `url` no item (bucket privado) — **PARCIAL/GATE:** campo `url` editável (colar link); migração de bucket privado escrita em `022_storage_documentos.sql` mas **NÃO aplicada** (gate humano). Upload real fica para follow-up.
  - [x] `updated_at` do item actualizado a cada mudança de estado
- [x] Timeline unificada (AC: 3)
  - [x] Agregador por `lead_id` sobre as 3 fontes com tipo de evento discriminado (`src/lib/timeline-agregador.ts`)
  - [x] Extensível: novo tipo de fonte (email E5) entra acrescentando 1 mapeador, sem reescrever o consumidor (fonte `'email'` já reservada no tipo discriminado)
- [x] Formulário público (AC: 4)
  - [x] Rota pública `src/app/ficha/page.tsx` (fora do grupo `(dashboard)`); adicionada a PUBLIC_PATHS no `src/proxy.ts`; sem dados de terceiros expostos
  - [x] Route handler server-side `src/app/api/ficha-publica/route.ts`: valida input, aplica `normalizeAngolaPhone`, faz upsert lead+ficha (dedup por telefone normalizado, `origem='formulario_site'`) e regista consentimento (`consentimentos`, tipo=`dados_pessoais`, fonte=`formulario`)
- [x] Toggle `processo_em_curso` com aviso de retenção (AC: 5)
- [x] Degradação graciosa dos painéis financeiro/emails ainda vazios (AC: 6)

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §2.5](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) (tabela `fichas_estudante` completa, incl. comentário "⚠ bloqueia a rotina de retenção de 2 anos"), §2.1 (enum `documento_estado`), §9 (rota `leads/[id]/`). PRD FR6 + §3 (ecrã nuclear nº4).
- **Dependência:** story 2.1 (migração 009 cria `fichas_estudante`); 2.2 para o selector de programa (não bloqueante); E1 story 1.2 para o guard de autenticação do grupo `(app)`.
- **Consumidores a jusante:** story 2.5 (importação escreve nestas fichas), E3 (tool `criar_ficha_estudante` do agente cria/actualiza a mesma ficha a partir da conversa — FR6; o formulário público é a via alternativa "link" do FR6), story 4.4 (retenção lê `processo_em_curso`).
- **Formulário público e consentimento:** FR24 exige consentimento registado por contacto (fonte, data) — o submit do formulário é um ponto de captura natural; a tabela `consentimentos` já existe (E1, migração 007). O PRD não o diz explicitamente no AC4 desta story — incorporado aqui por coerência com FR24 (desvio menor registado).
- **Checklist como JSONB, não tabela:** decisão da arquitectura (§2.5) — não criar tabela `documentos`; o enum `documento_estado` aplica-se por validação aplicacional (JSONB não tem CHECK por item).

### Testing
- node:test do agregador de timeline (ordenação cronológica, merge das 3 fontes, fonte vazia não quebra) e da validação de item de documento (estado fora do enum rejeitado).
- node:test do handler do formulário público: telefone `923 456 789` → normalizado; submissão repetida do mesmo telefone → upsert, não duplicado.
- SQL: `SELECT count(*) FROM fichas_estudante GROUP BY lead_id HAVING count(*)>1` → vazio (UNIQUE respeitado).
- Smoke UI: mudar estado de documento → persiste no JSONB com `updated_at` novo; toggle `processo_em_curso` mostra o aviso; formulário público submetido → lead + ficha + consentimento criados.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E2 + Arquitectura v1.0 | River (@sm) |
| 13/07/2026 | 1.1 | Execução @dev: ficha 360º (secções, checklist JSONB, timeline, toggle processo_em_curso, degradação graciosa), formulário público (rota `/ficha` + handler `/api/ficha-publica`), `normalize-phone` (paridade edge Deno) + 2 suites de teste (20 asserções). Build verde. Desvio resolvido pela coordenação: `leads.origem` alinhado ao enum §2.1 via migração 021 → formulário usa `origem='formulario_site'` directamente (sem contorno). Upload de storage deixado como GATE (`022_storage_documentos.sql`, não aplicado). | Dex (@dev) |
| 13/07/2026 | 1.2 | Coordenação: migrações 021 (enum origem)/022 (**tabela `consentimentos` em falta** — a arquitectura mapeava-a ao clone E1 mas nunca foi criada; apanhado no teste E2E do formulário) + 023 (bucket storage privado) aplicadas. Formulário público validado E2E contra a BD: origem=formulario_site, telefone normalizado, ficha 1:1, consentimento registado, deduplicação. Story Done. | Coordenação (@dev) |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (Dex / @dev)

### Debug Log References
- Testes novos (timeline-agregador + normalize-phone): 20/20 pass.
- `npm run build`: VERDE (Next 16.2.10, webpack). Rotas `/api/ficha-publica`, `/clientes/[id]`, `/ficha` (estática) presentes.
- `npm test` (suite completo): 21 pass / 10 fail — **as 10 falhas são pré-existentes** (testes de outras stories: cutover multi-tenant, shared-mappers, backend-config, go-live, manifest, service-worker) que dependem de exports/ficheiros ausentes neste worktree. Nenhuma toca ficheiros desta story.

### Completion Notes List
- **Ficha 360º** estende `src/app/(dashboard)/clientes/[id]/page.tsx` (não criou `leads/[id]`): a página server-side carrega ficha + catálogo de programas + as 3 fontes de timeline e passa ao componente client `Ficha360`. Secções implementadas: dados pessoais, encarregado, percurso, destino/programa (selector do catálogo + `destino_pretendido` livre), orçamento (faixa). Guardar faz `upsert` com `onConflict: 'lead_id'`.
- **Checklist documental** sobre `documentos JSONB` com validação aplicacional do enum `documento_estado` (`src/lib/documentos.ts`): mudar estado actualiza `updated_at` do item; adicionar tipos (sugestões da checklist base + livre); campo `url` editável (upload real = GATE).
- **Timeline unificada** via `src/lib/timeline-agregador.ts` — merge das 3 fontes, ordenação desc, tipo discriminado por `fonte`; extensível (fonte `'email'` já reservada para E5).
- **Toggle `processo_em_curso`** com aviso explícito na UI: "⚠ Enquanto activo, este contacto está EXCLUÍDO da rotina de retenção de 2 anos."
- **Degradação graciosa (AC6):** painéis de financeiro (2.6) e emails (5.x) mostram placeholders "disponível em breve" sem quebrar a ficha; ficha inexistente é tratada com `maybeSingle` + `fichaVazia`.
- **Formulário público (AC4):** `src/app/ficha/page.tsx` (fora de `(dashboard)`, em PUBLIC_PATHS) + handler `src/app/api/ficha-publica/route.ts` (service_role, valida input, normaliza telefone, dedup por telefone, `origem='formulario_site'`, consentimento `dados_pessoais`/`formulario`).
- **`normalize-phone`:** `src/lib/normalize-phone.ts` é a versão TS 1:1 do `normalizeAngolaPhone` das edge functions Deno.
- **Desvio resolvido (coordenação):** o CHECK original de `leads.origem` não aceitava `formulario_site`; a migração `021_leads_origem_enum.sql` (aplicada pela coordenação) alinhou-o ao enum §2.1 — o formulário usa `origem='formulario_site'` sem contorno.
- **GATE (não feito):** aplicação de `022_storage_documentos.sql` (bucket privado) e upload real de ficheiros.

### File List
**Novos:**
- `src/lib/normalize-phone.ts` — normalização telefone (paridade edge Deno)
- `src/lib/timeline-agregador.ts` — agregador da timeline unificada
- `src/lib/documentos.ts` — validação/estado da checklist documental (enum `documento_estado`)
- `src/components/clientes/ficha360.tsx` — componente client da ficha 360º
- `src/app/ficha/page.tsx` — formulário público (rota pública)
- `src/app/api/ficha-publica/route.ts` — route handler server-side do formulário público
- `supabase/migrations/022_storage_documentos.sql` — bucket privado de documentos (**GATE — não aplicar**)
- `tests/timeline-agregador.test.mjs` — testes do agregador + validação de documentos
- `tests/normalize-phone.test.mjs` — testes de normalização de telefone

**Modificados:**
- `src/app/(dashboard)/clientes/[id]/page.tsx` — estendida para carregar ficha + timeline + catálogo e montar `Ficha360`
- `src/types/database.ts` — tipos `FichaEstudante`, `DocumentoItem`, `DocumentoEstado`
- `src/proxy.ts` — `/ficha` e `/api/ficha-publica` adicionados a PUBLIC_PATHS

## QA Results
