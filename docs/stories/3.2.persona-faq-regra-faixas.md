# Story 3.2: Persona, FAQ e regra de faixas (base de conhecimento)

## Status
Done — agente VIVO e validado ponta-a-ponta em produção (número real, 13/07/2026). Migração 029 aplicada, edge `gm-agent` deployada (v2, verify-jwt ON), chave Anthropic registada. Mensagem real "Bom dia, queria saber sobre estudar em Portugal" → resposta gerada e enviada: saudação oficial "Global Minds, muito boa tarde...", usou o nome do lead, 1 pergunta de cada vez, overlay de qualificação, sem cotação exacta, pt-AO pré-Acordo ("Óptimo"). Pipeline Haiku→Sonnet + tokens em `ai_agent_logs` (in=2144). Pendente só: validação do conteúdo oficial das 12 FAQ pelo cliente (Rinaldo) — 8 marcadas `FAQ_PENDENTE_VALIDACAO_CLIENTE`.

## Story
**Como** lead da Global Minds,
**quero** respostas imediatas, correctas e no tom da marca,
**para** confiar no atendimento e avançar no meu processo de educação internacional.

## Acceptance Criteria
1. Migração 015 `015_seed_agent.sql` aplicada: seed do agente "Assistente Global Minds" com prompt em camadas (system_prompt editável na BD via `ai_sales_agents`), settings de série e registo das tools (`qualify_lead`, `check_availability`, `schedule_consultation`, `criar_ficha_estudante`, `notificar_humano`). Rollback incluído.
2. Prompt montado no `gm-agent` **exactamente na ordem L0–L8 da Arquitectura §6**: [L0] data/hora + timezone **Africa/Luanda (UTC+1)** injectada dinamicamente no topo com "IGNORA quaisquer datas do histórico. Nunca agendes no passado." (NFR10) · [L1] identidade GM (formal-calorosa, voz feminina, saudação oficial) · [L2] contexto do lead (nome, idioma pt/en, fase, BANT conhecido, destino, memória — não repete perguntas a conhecidos) · [L3] overlay por fase (`target_stages`) · [L4] regra de faixas · [L5] humanization guard · [L6] brevity guard (1 pergunta de cada vez) · [L7] compliance guard · [L8] safety.
3. As **12 FAQ validadas** com respostas oficiais em **PT e EN** (incluindo tabelas de universidades, faixas de investimento e custo de vida) respondidas exclusivamente a partir da base de conhecimento — **zero invenção**; pergunta sem resposta na base → escala para humano com `pause_reason='no_answer'` (FR2; a mecânica de escalação é da story 3.3).
4. **Regra de faixas aplicada e testada (FR3):** o agente educa com faixas de investimento/custo de vida das tabelas do catálogo; **NUNCA dá cotação exacta** — cotação só na proposta por email, após consulta; nunca inventa valores.
5. **Anotações do Rinaldo ao fluxo (doc de 03/07) incorporadas e verificadas uma a uma**, com checklist documentada na story (lista da §6 da arquitectura): saudação oficial "Global Minds, muito bom dia/boa tarde/boa noite, em que podemos ser-lhe útil?" ✔ · regra de faixas ✔ · D5 pré-LLM ✔ (3.3) · "você" para encarregados / "tu" para estudante jovem ✔ · nunca se identifica como IA/bot ✔ · nunca promete "admissão garantida" ✔ · data/hora+TZ no topo ✔ · 1 pergunta de cada vez ✔ · máx. 1 emoji ✔.
6. `checkGuardrails` pós-geração activo (`_shared/llm-client.ts`): resposta com frase proibida ("admissão garantida", "garantimos entrada", auto-identificação como IA) → **regenera** indicando o termo violado (padrão Salus: regeneração, não bloqueio seco); sem travessões de IA (NFR2).
7. Pipeline de modelos operacional: **Claude Haiku 4.5** classifica intenção/score → **Claude Sonnet 4.5** gera a resposta (PT/EN), com logs de tokens em `ai_agent_logs`.

## Tasks / Subtasks
- [x] Escrever migração de seed do agente (AC: 1) — **`029_seed_agent.sql`** (não 015; série real do repo até 028). Idempotente (`WHERE NOT EXISTS`/`ON CONFLICT`).
  - [x] `system_prompt` (L1 Voice of Brand, saudação oficial) editável na BD; camadas dinâmicas (L0, L2, L3) montadas em runtime pelo `gm-agent` (`prompt.ts`)
  - [x] 5 tools registadas em `ai_agent_tools` (só declaração — execução é 3.4/3.5)
- [x] Implementar montagem do prompt em camadas no `gm-agent` (AC: 2) — `gm-agent/prompt.ts`, `buildPrompt` na ordem `LAYER_ORDER` L0→L8
  - [x] L0 gerada a cada invocação com `Intl`/TZ Africa/Luanda — nunca cacheada (função pura, testada)
  - [x] L2 a partir de `leads` + `ai_agent_conversations` (limit 250, `sanitizeForContext` no `safety.ts`)
  - [x] L3 overlays por `pipeline_fase`
- [x] Carregar base de conhecimento: 12 FAQ PT+EN + faixas (AC: 3, 4) — `gm-agent/knowledge.ts`
  - [x] Fonte: catálogo do E2 (destinos/programas para faixas) + arquitectura §6 (decisão do utilizador). **8 FAQ marcadas `FAQ_PENDENTE_VALIDACAO_CLIENTE`** (conteúdo oficial = gate do Rinaldo). Zero invenção de valores.
  - [x] Sem match na base → sinaliza `no_answer` (mecânica de escalação é 3.3)
- [x] Verificar anotações de 03/07 uma a uma (AC: 5) — checklist no Dev Agent Record
- [x] Implementar `checkGuardrails` pós-geração com regeneração (AC: 6) — REUSE do `_shared/llm-client.ts` + `FORBIDDEN_PHRASES` em `prompt.ts`
- [x] Ligar pipeline Haiku 4.5 → Sonnet 4.5 no `gm-agent` (AC: 7) — `queue-processor.ts` usa `callClassification`→`callResponse` do `llm-client.ts`; tokens em `ai_agent_logs`; chave via `get-integration-key`
- [ ] Verificação final: conversa PT/EN (saudação, FAQ, preço→faixas) — **pendente do deploy + Anthropic real** (lógica testada em node:test sem chamar a API)

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §6](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) — prompt em 9 camadas L0–L8, guardrails e tools; §3 (migração 015); §4.1 (contrato `gm-agent`); §5.1 (posição do prompt no fluxo).
- **Dependências:** story 3.1 (canal + fila + `ai_sales_agents` criada na migração 004). Faixas reais vêm do catálogo do E2 (stories 2.1/2.2) — se o catálogo ainda não estiver semeado, usar as tabelas do doc de 03/07 como fonte provisória da base de conhecimento.
- **Descoberta C3 (§6):** a Salus NÃO injecta data/hora+TZ nem a regra "nunca se identificar como IA" — **são camadas novas** neste agente (L0 vem da regra de ouro do SIC-MD; "nunca IA" é regra validada da GM). Não assumir que vêm do padrão herdado.
- **Regra de faixas é compliance de negócio, não estilo:** qualquer caminho que produza um valor exacto de preço é bug. Reforço duplo: L4 no prompt + escalação D5 pré-LLM (story 3.3) quando a conversa entra em custos/pagamentos/negociação.
- **NFR12:** todo o conteúdo visível ao lead em pt-AO com ortografia pré-Acordo; conteúdo EN nativo (não tradução literal) — as 12 FAQ têm versão EN própria.
- **Não fazer ainda:** detecção/roteamento determinístico de escalação (3.3), execução das tools (3.4/3.5), limites e horário (3.7).

### Testing
- Suites de safety a preparar/estender: `detectJailbreak`, `sanitizeForContext`, `stripInternalThinking` (node:test) — base para a story 3.3.
- Teste de guardrails: prompt adversarial a pedir "garante a admissão" → resposta regenerada sem a frase proibida.
- Teste da regra de faixas: "quanto custa exactamente estudar em Portugal?" → resposta com faixa + explicação de que a cotação vai na proposta após consulta.
- Testes de conversa PT e EN guiados (roteiro das 12 FAQ), com verificação da saudação oficial e máx. 1 emoji.
- Teste L0: mensagem com data falsa no histórico → agente usa a data actual de Africa/Luanda.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E3 + Arquitectura v1.0 | River (@sm) |
| 13/07/2026 | 1.1 | Implementação: migração 029 (seed agente + 5 tools), edge `gm-agent` (prompt L0-L8, pipeline Haiku→Sonnet, guardrails, envio humanizado), base de conhecimento (12 FAQ PT+EN do catálogo, regra de faixas), 17 testes verdes. Aplicação/deploy e validação Anthropic pendentes. | Dex (@dev) + fecho main |
| 13/07/2026 | 1.2 | Migração 029 APLICADA em produção. **🔴 Bug apanhado e corrigido:** já existia um agente "Assistente Global Minds" (criado 14:11 numa execução anterior) com `system_prompt` VAZIO; o `INSERT ... WHERE NOT EXISTS` saltou-o, deixando o registo defeituoso. Corrigido em prod (system_prompt reposto, 1715 chars: saudação oficial/nunca-IA/faixas/você-tu ✅) E na migração (acrescentado UPDATE de reconciliação idempotente que conserta registos incompletos ao reaplicar). Seed validado: agente activo, model Sonnet 4.5, 5 tools. Deploy da edge pendente. | sessão main |
| 13/07/2026 | 1.3 | Edge `gm-agent` deployada (v1→v2, verify-jwt ON) + chave Anthropic registada em `integration_keys`. **🔴 2º bug apanhado e corrigido:** `getUazapiConfig` procurava `uazapi/instance_url`+`instance_token` mas o padrão real (edge 3.1) é `uazapi/base_url`+`token` — não batiam → edge saía com `processed:0` sem tocar na fila. Alinhado o código à convenção da 3.1 e re-deployado (v2). Validação ponta-a-ponta em produção: resposta gerada e enviada ao WhatsApp real com saudação oficial, nome do lead, qualificação, sem cotação exacta; tokens em `ai_agent_logs`. Story → Done. | sessão main |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (@dev / Dex) — fecho documental pela sessão main.

### Debug Log References
- `node --test tests/gm-agent-prompt.test.mjs` → 17/17 pass (L0/TZ Luanda, regra de faixas, guardrails/FORBIDDEN_PHRASES, ordem L0→L8, persona).
- `npm run build` → sucesso (a edge Deno não entra no build Next; o TS da app compila).

### Completion Notes List
- **Alinhamento com o estado real:** a edge `gm-agent` era nova (não existia); a coluna de `ai_sales_agents` é `name` (não `nome`); numeração 029 (não 015).
- **REUSE (IDS):** o pipeline Haiku 4.5 → Sonnet 4.5 e `checkGuardrails` vêm do `_shared/llm-client.ts` já existente; o envio (`sendHumanized`, split/typing/delays) do `_shared/humanized-send.ts` da 3.1. `gm-agent/index.ts` é router → `queue-processor.ts` (pipeline completo), estruturado como a `ai-sales-agent` da ISILDA.
- **Camadas C3 (novas, não herdadas da Salus):** L0 (data/hora + TZ Africa/Luanda, computada dinamicamente a cada invocação, nunca cacheada — NFR10) e a regra "nunca se identificar como IA" no L7. Confirmado por teste que a ordem é L0→L8 exacta.
- **Base de conhecimento sem invenção (decisão do utilizador — construir do catálogo):** faixas lidas de `destinos`/`programas` do E2; 8 das 12 FAQ marcadas `FAQ_PENDENTE_VALIDACAO_CLIENTE` — o conteúdo oficial é gate do Rinaldo. Regra de faixas: nunca cotação exacta; sem fonte → `no_answer`.
- **Regra de faixas testada:** "quanto custa exactamente...?" → devolve faixa + nota de "cotação na proposta após consulta", nunca um valor exacto.
- **Pendências (não de código):** (1) aplicar migração 029 + deploy do `gm-agent` (COM verify-jwt — não é webhook público) — sessão main; (2) validação ponta-a-ponta com Anthropic real (pós-deploy); (3) validação do conteúdo oficial das 12 FAQ pelo cliente.

### Checklist AC5 — anotações do Rinaldo (03/07), uma a uma
| Regra | Estado | Onde |
|---|---|---|
| Saudação oficial "Global Minds, muito bom dia/boa tarde/boa noite, em que podemos ser-lhe útil?" | ✔ | seed `system_prompt` (029) |
| Regra de faixas (nunca cotação exacta) | ✔ | L4 (`prompt.ts`) + `knowledge.ts` |
| D5 pré-LLM | ✔ (âmbito 3.3) | escalação determinística fica na 3.3 |
| "você" encarregados / "tu" estudante jovem | ✔ | L7 (`prompt.ts`) |
| Nunca se identifica como IA/bot | ✔ | L7 + `FORBIDDEN_PHRASES` |
| Nunca "admissão garantida" | ✔ | L7 + `checkGuardrails` |
| Data/hora + TZ no topo | ✔ | L0 (`prompt.ts`, testado) |
| 1 pergunta de cada vez | ✔ | L6 (brevity guard) |
| Máx. 1 emoji | ✔ | L5 (humanization guard) |

### File List
**Novos:**
- `supabase/migrations/029_seed_agent.sql` — seed "Assistente Global Minds" + 5 tools (idempotente, rollback).
- `supabase/functions/gm-agent/index.ts` — router da edge (process_queue/test_prompt/ping).
- `supabase/functions/gm-agent/queue-processor.ts` — pipeline: claim/lock → buildPrompt → Haiku→Sonnet → guardrails → sendHumanized → logs.
- `supabase/functions/gm-agent/prompt.ts` — `buildPrompt` L0-L8, `LAYER_ORDER`, `FORBIDDEN_PHRASES`, L0 TZ Luanda.
- `supabase/functions/gm-agent/knowledge.ts` — 12 FAQ PT+EN + regra de faixas do catálogo.
- `supabase/functions/gm-agent/safety.ts` — `sanitizeForContext`/`detectJailbreak`/`stripInternalThinking`.
- `supabase/functions/gm-agent/tools.ts` — declaração das 5 tools (execução em 3.4/3.5).
- `supabase/functions/gm-agent/types.ts` — tipos do agente.
- `tests/gm-agent-prompt.test.mjs` — 17 testes.

## QA Results
