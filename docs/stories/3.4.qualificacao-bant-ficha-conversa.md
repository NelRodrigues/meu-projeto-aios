# Story 3.4: Qualificação BANT e ficha em conversa

## Status
Done — validado ponta-a-ponta em produção (13/07/2026). Conversa de qualificação real ("sou encarregado, licenciatura em Portugal, Setembro, temos orçamento") → BANT extraído (authority=encarregado, need=licenciatura em Portugal, timeline=Setembro, destino=Portugal, nivel=licenciatura), `sales_score=80`, `confidence=medium`, `temperature=quente` (trigger conservador), `pipeline_fase=qualificado` + `mudancas_estagio`, **alerta de lead quente** ao Rinaldo com a **conversa a continuar ACTIVE** (distinto da escalação D5). Edges `gm-lead-intelligence` v1 + `gm-agent` v7. 57 testes verdes.

## Story
**Como** Rinaldo,
**quero** leads qualificados com ficha criada automaticamente durante a conversa,
**para** decidir rápido quem merece Consulta de Orientação.

## Acceptance Criteria
1. Tool `qualify_lead` (Anthropic tool-use, loop máx. 4 iterações) invoca a edge `gm-lead-intelligence`: **Claude Haiku 4.5** extrai BANT da conversa (`bant_budget`, `bant_authority`, `bant_need`, `bant_timeline`) + destino, nível, orçamento (faixa declarada, nunca valor exacto) e prazo — recolhidos **sem interrogatório, 1 pergunta de cada vez** (guard L6).
2. `sales_score` **0–100** + `score_confidence` (`low`/`medium`/`high`) + `fit_score` calculados e gravados em `leads`; `temperature` derivada por trigger BEFORE INSERT/UPDATE (`quente ≥70`, `morno 40–69`, `frio <40`; **nunca `quente` com `score_confidence='low'`** — regra conservadora herdada do SIC-MD).
3. Alerta de lead quente (FR5): `sales_score ≥ 70 AND score_confidence <> 'low'` → `notificar_humano` (WhatsApp ao Rinaldo com resumo + link CRM); lead movido de fase automaticamente (`pipeline_fase='qualificado'` com registo em `mudancas_estagio`).
4. Tool `criar_ficha_estudante` cria/actualiza `fichas_estudante` a partir dos dados recolhidos na conversa (nome, encarregado, percurso, destino/programa pretendido, faixa de orçamento) — FR6; alternativa por link de formulário público (E2 story 2.4) oferecida quando o lead prefere.
5. Lead sem objectivos E sem capacidade → **porta aberta honesta**: agente encerra com cordialidade sem forçar avanço; lead não muda de fase (fica elegível para reactivação a 90 dias — E4).
6. Degradação graciosa (NFR11): falha de classificação mantém o perfil anterior do lead (nunca sobrescreve com vazio); tool sem texto de resposta → **resposta de continuidade** ao lead, nunca fallback técnico; falha de tool não quebra o loop.

## Tasks / Subtasks
- [x] Implementar edge `gm-lead-intelligence` (AC: 1, 2, 6) — `supabase/functions/gm-lead-intelligence/index.ts`
  - [x] Contrato §4.1: entrada `{lead_id, conversation_ctx}` → saída `{score, confidence, temperature}`
  - [x] Haiku 4.5 extrai BANT; cálculo de `sales_score`/`score_confidence`/`fit_score` (régua pura em `lead-intelligence.ts`); UPDATE `leads`
  - [x] Em falha do modelo: log em `ai_agent_logs`, **perfil anterior mantido** (nunca sobrescreve com vazio), erro não propagado ao lead
- [x] Ligar tools `qualify_lead` e `criar_ficha_estudante` no `gm-agent` (AC: 1, 4, 6) — `tools-exec.ts` substitui `executeToolPlaceholder`
  - [x] Loop de tools máx. 4 iterações (já existia); as outras 3 tools mantêm placeholder (3.5)
  - [x] Resposta de continuidade pós-tool obrigatória (padrão Salus)
- [x] Trigger de temperatura + alerta quente (AC: 2, 3)
  - [x] Trigger da migração 010 **JÁ existia e valida** a regra "nunca quente com confiança baixa" (testado em prod: 85+low→morno, 85+medium→quente) — não tocado
  - [x] `≥70 & conf≠low` → `notificar_humano` (REUSE da 3.3) + `pipeline_fase='qualificado'` + INSERT `mudancas_estagio`. **Distinto da escalação D5:** NÃO transfere — a conversa continua activa; é um alerta positivo.
- [x] Comportamento de porta aberta (AC: 5) — critério no overlay L3 da fase `lead` (`prompt.ts`) + mensagem honesta; lead não muda de fase
- [ ] Verificação final: conversa lead→qualificado com ficha + alerta — **pendente do deploy** (lógica validada em 19 testes; ponta-a-ponta é da sessão main)

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §4.1](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) — contrato `gm-lead-intelligence`; §2.4 (colunas de qualificação do `leads` + trigger de temperatura + threshold do alerta); §6 (tools do agente e loop máx. 4); §5.1 (Haiku classifica → Sonnet responde).
- **Dependências:** stories 3.1 (fila) e 3.2 (prompt/overlays/registo de tools na 015); **E2 stories 2.1/2.4** — a migração 010 (`ALTER leads` com BANT/score/confidence/temperature) e a 009 (`fichas_estudante`) têm de estar aplicadas antes desta story. Todas dependem do E1 e da migração 004.
- **Conflito C1 (§2.4 — não esquecer):** o SIC-MD **não tem** `score_confidence`, `fit_score` nem `temperature` como colunas — são campos novos desenhados nesta arquitectura. Não copiar cegamente do SIC-MD; a definição vinculativa é a da migração 010.
- **Escalas:** o brief usa temperatura 0–10 (QUENTE ≥9); a arquitectura normalizou para 0–100 (`quente ≥70`, `morno 40–69`, `frio <40`). A escala 0–100 é a vinculativa.
- **Regra de faixas atravessa esta story:** `orcamento` guarda a faixa declarada pelo lead, nunca um valor cotado pelo agente (L4, story 3.2).
- **Modelos:** Haiku 4.5 para extracção/classificação (barato, rápido); Sonnet 4.5 continua a conduzir a conversa. Chaves via `integration_keys`.

### Testing
- node:test da `gm-lead-intelligence`: fixtures de conversa → BANT extraído, score/confidence/temperature coerentes; falha do modelo → perfil anterior intacto.
- Teste do trigger: `sales_score=85, confidence='low'` → temperature ≠ 'quente' e SEM alerta; `85 + 'medium'` → 'quente' + alerta.
- Teste de continuidade: tool devolve vazio → lead recebe mensagem natural (nunca erro técnico); loop nunca excede 4 iterações.
- Teste da ficha: conversa com dados parciais → `fichas_estudante` criada com campos preenchidos e restantes NULL (sem inventar).
- Conversas de teste PT/EN: qualificação completa em ambos os idiomas.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E3 + Arquitectura v1.0 | River (@sm) |
| 13/07/2026 | 1.1 | Implementação: edge `gm-lead-intelligence` (Haiku extrai BANT → score/confidence/fit → UPDATE leads), execução real de `qualify_lead`/`criar_ficha_estudante` (`tools-exec.ts` substitui placeholder), alerta de lead quente (notifica + muda fase, sem transferir), porta aberta honesta (L3), 19 testes verdes. Deploy/validação produção pendentes. | Dex (@dev) + fecho main |
| 13/07/2026 | 1.2 | Deploy (`gm-lead-intelligence` v1, `gm-agent` v4→v7). Validação ponta-a-ponta em produção revelou e corrigiu **3 problemas de comportamento que só o teste real com o agente vivo apanha**: (1) **🔴 `no_answer` demasiado agressivo** — o detector de escalação transferia mensagens de qualificação declarativas como "perguntas sem resposta" (agravado por 8/12 FAQ em esqueleto); fix em `escalation.ts` (`no_answer` só escala PERGUNTAS, via novo `isQuestion`) + 4 testes. (2) **🔴 tools não accionadas** — o Sonnet conversava mas não chamava `qualify_lead`/`criar_ficha_estudante`; fix no overlay L3 do `prompt.ts` (instrui o uso das tools). (3) **rótulo enganador** — a notificação de lead quente dizia "Transferência:" (é alerta, não transferência); fix em `handoff.ts` ("Alerta:" para lead_quente). Após os fixes: BANT extraído+score 80+quente+fase qualificado+alerta com conversa activa, tudo confirmado em produção. 57 testes verdes. Story → Done. | sessão main |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (@dev / Dex) — fecho documental pela sessão main.

### Debug Log References
- `node --test tests/qualificacao-bant.test.mjs` → 19/19 pass (régua score/confidence/fit, condição do alerta ≥70 & ≠low, ficha sem inventar, `buildFichaFormLink`, continuidade pós-tool).
- `npm run build` → sucesso.

### Completion Notes List
- **Estado real aproveitado:** a edge `gm-lead-intelligence` era nova; o **trigger de temperatura JÁ existia** (migração 010) e valida a regra conservadora — testado em produção (85+low→morno, 85+medium→quente), por isso NÃO foi tocado. As colunas de qualificação em `leads` já existiam. As tools já estavam registadas (029). O loop de tools já existia no `gm-agent` (usava `executeToolPlaceholder`).
- **Execução real de tools:** `tools-exec.ts` implementa `qualify_lead` (invoca `gm-lead-intelligence` → score/UPDATE) e `criar_ficha_estudante` (upsert em `fichas_estudante`, campos sem dado ficam NULL — no-fallbacks/AC4). As outras 3 tools (`check_availability`/`schedule_consultation`/`notificar_humano`) mantêm placeholder até 3.5/pré-LLM.
- **🎯 Distinção alerta-lead-quente vs escalação-D5 (crítica, bem resolvida):** o alerta de lead quente (`sales_score≥70 AND confidence≠low`) NOTIFICA o Rinaldo (REUSE `notificar_humano`/handoff) + move `pipeline_fase='qualificado'` + regista `mudancas_estagio`, mas **NÃO transfere** — a conversa fica `active` e o agente continua. Isto é o OPOSTO da escalação D5 (3.3), que transfere e cala o agente. Documentado no cabeçalho de `tools-exec.ts`.
- **Régua de score (documentada, defensável):** score sobe com completude do BANT + fit ao catálogo; confidence sobe com quantidade/qualidade de sinais extraídos; nunca inventa. A temperatura deriva do trigger (não é calculada na edge).
- **Degradação graciosa (AC6):** falha do modelo → mantém perfil anterior (não sobrescreve com vazio); tool vazia → resposta de continuidade natural (nunca fallback técnico); loop respeita máx. 4.
- **Pendências (não de código):** deploy da edge `gm-lead-intelligence` + re-deploy `gm-agent` (v4, com `tools-exec`) — sessão main; validação ponta-a-ponta (conversa de qualificação real → BANT extraído + ficha criada + alerta ao Rinaldo).

### File List
**Novos:**
- `supabase/functions/gm-lead-intelligence/index.ts` — edge de inteligência de lead (Haiku → BANT → score → UPDATE leads; degradação graciosa).
- `supabase/functions/gm-agent/tools-exec.ts` — executor real de `qualify_lead` + `criar_ficha_estudante` (+ alerta de lead quente, distinto de D5).
- `supabase/functions/gm-agent/lead-intelligence.ts` — lógica pura (régua score/confidence/fit, montagem de ficha, `buildFichaFormLink`).
- `tests/qualificacao-bant.test.mjs` — 19 testes.

**Modificados:**
- `supabase/functions/gm-agent/queue-processor.ts` — usa `executeTool` (real) em vez de `executeToolPlaceholder`.
- `supabase/functions/gm-agent/prompt.ts` — porta aberta honesta no overlay L3 da fase `lead`.
- `supabase/functions/gm-agent/handoff-format.ts` — ajuste para reutilização no alerta de lead quente.

## QA Results
