# Story 3.3: Escalação determinística e handoff (regra D5)

## Status
Done — validado ponta-a-ponta em produção (13/07/2026). "Posso pagar em 2 vezes?" → conversa `transferred`/`escalation_d5`, resposta fixa de transição (SEM o LLM falar de valores), notificação `takeover` "Transferência: Custos/pagamento/negociação (regra D5)". Edges `gm-agent` v3 + `uazapi-webhook-receiver` v2 deployadas. **Bónus: corrigido bug do cron** (`process-ai-agent-queue` não disparava — dependia de `ALTER DATABASE` sem privilégios; migração 030 fá-lo ler config de `integration_keys`; confirmado a processar a fila sozinho em ~100s).

## Story
**Como** Rinaldo,
**quero** que o agente me passe a conversa nos momentos críticos,
**para** nunca deixar a IA falar de dinheiro ou tratar casos graves sozinha.

## Acceptance Criteria
1. Verificação de escalação executada no `gm-agent` **ANTES de qualquer chamada ao LLM** (determinística — princípio 6 da arquitectura), com gatilhos hard (FR4): pedido de humano ("falar com humano"/"pessoa") → `human_request` · urgência/problema grave → `urgent` · **custos de serviços, pagamentos, valores ou negociação (regra D5)** → `escalation_d5` · pergunta sem match na base de conhecimento → `no_answer`.
2. Quando um gatilho dispara: `UPDATE ai_agent_conversations SET status='transferred'` + `pause_reason` correcto (valores exactos do enum §2.1: `opt_out`, `human_request`, `escalation_d5`, `urgent`, `no_answer`, `manual`); o LLM **não é chamado**; o lead recebe resposta fixa de transição ("vou passar a um consultor").
3. Handoff notifica Rinaldo/Ana por WhatsApp com **resumo da conversa + link directo para o CRM** (tool/função `notificar_humano`), e a notificação fica registada no CRM (tabela `notificacoes` — FR26).
4. Resposta humana enviada pelo número da GM pausa automaticamente o agente nessa conversa (`status='paused_by_human'`, setting `auto_pause_after_human_reply`) — detectada no receiver (fromMe/owner) e no envio manual pela inbox (story 3.6).
5. Escalação soft: N mensagens sem avanço de fase (desde o último avanço) → agente sugere handoff (não transfere à força; N configurável nos settings).
6. Jailbreak e injecção de prompt detectados e neutralizados: `sanitizeForContext` aplicado ao histórico (L8), `detectJailbreak` na mensagem entrante, `stripInternalThinking` na saída — **suite de safety a passar** (NFR5).

## Tasks / Subtasks
- [x] Implementar detector determinístico pré-LLM no `gm-agent` (AC: 1, 2) — `escalation.ts` (`detectEscalation`/`resolveEscalation`)
  - [x] Listas de gatilhos PT+EN para `human_request`, `urgent` e `escalation_d5` (D5_PATTERNS: prestações/sinal/IBAN/desconto/valor exacto)
  - [x] `no_answer`: integra com `matchFaq` do `knowledge.ts` (3.2) — sem match E não-social → sinaliza
  - [x] Ordem no fluxo: no `processOne`, depois do claim/lock, ANTES de `generateResponse` (pré-LLM)
- [x] Transferência e resposta fixa (AC: 2)
  - [x] `status='transferred'` + `pause_reason`; mensagem de transição fixa (PT/EN) via `sendHumanized`; o LLM NÃO é chamado
- [x] Implementar mecânica de `notificar_humano` (AC: 3) — `handoff.ts` + `handoff-format.ts`
  - [x] Resumo (últimas msgs `sanitizeForContext`) + link `<site_url>/inbox?lead=<id>`
  - [x] Envio WhatsApp ao Rinaldo/Ana (números: `settings.notify_numbers` → `team_members` admin com telefone → `[]` sem inventar) + INSERT em `notificacoes`
  - [x] Tool `notificar_humano` já registada na migração 029 (3.2)
- [x] Pausa por resposta humana (AC: 4) — `handleHumanReply` no receiver
  - [x] Mensagem fromMe/owner numa conversa activa → `paused_by_human` (respeita `auto_pause_after_human_reply`)
- [x] Escalação soft (AC: 5) — `soft_escalation_after_messages` nos settings; dica no prompt (sugere, não força)
- [x] Safety: `detectJailbreakAttempt`, `sanitizeForContext`, `stripInternalThinking` ligados ao fluxo (AC: 6)
- [ ] Verificação final em produção: "posso pagar em 2 vezes?" → transferida SEM resposta do LLM sobre valores — **pendente do deploy** (lógica validada em 34 testes; ponta-a-ponta é da sessão main)

## Dev Notes
- **Fonte vinculativa:** [Arquitectura §5.2](../architecture/ARCHITECTURE-SIC-GLOBAL-MINDS.md) — fluxo de escalação D5 completo (hard + soft); §2.1 (enum `pause_reason` e `conversa_status`); §4.1 (posição no contrato do `gm-agent`); §6 L8 (safety) e nota C3.
- **Dependências:** stories 3.1 (fila/conversas/`notificacoes`) e 3.2 (base de conhecimento para o gatilho `no_answer`; prompt onde o L8 se encaixa). Todas dependem do E1 e da migração 004.
- **Porquê pré-LLM:** a regra D5 é compromisso contratual validado pelo cliente — falar de dinheiro NUNCA pode depender do julgamento do modelo. O detector é código determinístico; o LLM nem sequer vê a mensagem quando dispara.
- **Distinção opt-out vs escalação:** o opt-out "SAIR" corre ainda mais cedo (no receiver, story 3.1); a escalação corre no processador da fila. Ambos partilham o enum `pause_reason`.
- **Cuidado com falsos positivos D5:** perguntas sobre faixas de investimento (story 3.2, L4) são legítimas para o agente; o gatilho D5 é para negociação/pagamento/cotação de serviços da GM. Documentar a fronteira nos testes (ex.: "qual a faixa para o Reino Unido?" → responde com faixa; "posso pagar em 2 vezes?" → escala).
- **Modelos:** quando não escala, o fluxo segue Haiku 4.5 (classificação) → Sonnet 4.5 (resposta) — story 3.2.

### Testing
- Suite de safety (node:test): `detectJailbreak` (tentativas de override de instruções PT/EN), `sanitizeForContext` (injecção via histórico), `stripInternalThinking` (fuga de raciocínio interno) — obrigatória antes do go-live (NFR5/NFR7).
- Testes do detector D5: matriz de frases (PT/EN) → `pause_reason` esperado; frases de faixas NÃO disparam.
- Teste de handoff: escalação → notificação WhatsApp com resumo + link, registo em `notificacoes`, conversa `transferred`.
- Teste `auto_pause_after_human_reply`: mensagem fromMe → conversa `paused_by_human`, fila não processa mais essa conversa.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 10/07/2026 | 1.0 | Story criada a partir do PRD E3 + Arquitectura v1.0 | River (@sm) |
| 13/07/2026 | 1.1 | Implementação: detector D5 determinístico pré-LLM (`escalation.ts`), handoff com resumo+link (`handoff.ts`/`handoff-format.ts`), pausa por resposta humana no receiver, escalação soft, safety reforçado. 34 testes verdes (matriz PT/EN + fronteira faixas-vs-D5). Deploy/validação produção pendentes. | Dex (@dev) + fecho main |
| 13/07/2026 | 1.2 | Edges deployadas (`gm-agent` v3, `uazapi-webhook-receiver` v2). Validação ponta-a-ponta em produção: "posso pagar em 2 vezes?" → `transferred`/`escalation_d5` + transição fixa + notificação `takeover` (o LLM NÃO falou de valores). **🔴 Bug de infra apanhado e corrigido:** o cron `process-ai-agent-queue` (028) dependia de `current_setting('app.*')` via `ALTER DATABASE`, que exige OWNER (42501 permission denied ao `db push`) — nunca foi definido, cron nunca disparava. Migração **030** cria `cron_config()` (SECURITY DEFINER a ler de `integration_keys` service='cron', sem segredo no git) e reagenda o cron. Confirmado: fila processada automaticamente em ~100s sem invocação manual. Story → Done. | sessão main |

## Dev Agent Record
### Agent Model Used
claude-opus-4-8 (@dev / Dex) — fecho documental pela sessão main.

### Debug Log References
- `node --test tests/escalacao-d5.test.mjs` → 34/34 pass (detector D5 PT/EN, fronteira faixas-vs-D5, safety, handoff-format).
- `node --test tests/gm-agent-prompt.test.mjs` → 17/17 (sem regressão da 3.2).
- `npm run build` → sucesso.

### Completion Notes List
- **REUSE (IDS):** `safety.ts` (`detectJailbreakAttempt`/`sanitizeForContext`/`stripInternalThinking`) e a tool `notificar_humano` já existiam da 3.2 — estendidos, não recriados. O detector integra-se no `processOne` existente.
- **Ponto de inserção pré-LLM:** `resolveEscalation` corre no `processOne` depois do claim/lock e ANTES de `generateResponse` (buildPrompt→Haiku→Sonnet). Quando dispara um gatilho hard, o LLM NÃO é chamado — `status='transferred'`, `pause_reason` do enum (§2.1, confirmado em produção que aceita os 6 valores), resposta fixa de transição.
- **🎯 Fronteira faixas-vs-D5 (o ponto nevrálgico):** `D5_PATTERNS` captura negociação/pagamento (prestações, sinal, IBAN, desconto, "quanto pago exactamente", "installments", "payment plan"); um `FAIXA_SAFE_PATTERNS` **isenta** perguntas de faixa legítimas (`faixa`, `faixa de investimento/preço/custo`, `investment/price range`) MESMO quando contêm palavras de dinheiro. Testado: "qual a faixa de preços?" → NÃO escala (agente responde com faixa, L4); "posso pagar em 2 vezes?" → `escalation_d5`. É uma allowlist explícita, não heurística frágil.
- **Handoff — números sem invenção (no-fallbacks):** origem por ordem — `settings.notify_numbers` → `team_members` (role=admin, is_active, telefone) → `[]` (loga que faltam números, NÃO inventa). Resumo via `sanitizeForContext`; link `<site_url>/inbox?lead=<id>`; INSERT em `notificacoes` (tipo `takeover`).
- **AC4 — dilema fromMe:** o próprio agente também envia como `fromMe`. Sem sinal fiável para distinguir agente-vs-humano no payload uazapi, a decisão (conforme brief) é: uma resposta manual pelo número da GM pausa a conversa (`paused_by_human`) — é o comportamento desejado (o operador assumiu a conversa). Respeita `auto_pause_after_human_reply`.
- **Pendências (não de código):** deploy do `gm-agent` (v3) + `uazapi-webhook-receiver` (v2) — sessão main; validação ponta-a-ponta em produção ("posso pagar em 2 vezes?" → transfere sem valores + notifica Rinaldo).

### File List
**Novos:**
- `supabase/functions/gm-agent/escalation.ts` — detector determinístico D5 (hard: human_request/urgent/escalation_d5; no_answer via knowledge; fronteira faixas-safe).
- `supabase/functions/gm-agent/handoff.ts` — mecânica de `notificar_humano` (resolução de números, envio WhatsApp, INSERT notificacoes).
- `supabase/functions/gm-agent/handoff-format.ts` — montagem pura do resumo + link (testável).
- `tests/escalacao-d5.test.mjs` — 34 testes.

**Modificados:**
- `supabase/functions/gm-agent/queue-processor.ts` — `resolveEscalation` pré-LLM + `performHandoff` + escalação soft.
- `supabase/functions/gm-agent/safety.ts` — reforço.
- `supabase/functions/uazapi-webhook-receiver/index.ts` — `handleHumanReply` (pausa por resposta humana).

## QA Results
