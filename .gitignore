

# Environment & Secrets (NEXUS)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (NEXUS)
node_modules/
node_modules

# Build & Logs (NEXUS)
dist/
build/
*.log
logs/

# IDE & OS (NEXUS)
.DS_Store
Thumbs.db
.idea/
*.swp

# NEXUS Local (NEXUS)
.aios-core/local/
.claude/settings.local.json
.aios/install-log.txt

# Standalone project repos (managed independently)
CLONE-NELSON/
clone-nelson/
PROJECTOS/Porcelana Beauty/SIC-PORCELANA/
